# Routine App 배포 가이드

루틴 앱을 설치 파일(.exe)로 만드는 방법입니다.

## 1. 빌드 준비

이미 `electron-builder` 설정을 완료해두었습니다.

## 2. 배포용 파일 생성 실행

터미널에서 아래 명령어를 입력하세요:

```powershell
npm run dist
```

## 3. 결과 확인

명령어가 끝나면 프로젝트 폴더 안에 `dist`라는 새 폴더가 생깁니다.

- `MyRoutine Setup.exe`: 설치형 파일
- `MyRoutine Portable.exe`: 설치 없이 바로 실행 가능한 파일

## 주의사항

- `icon.png` 파일이 프로젝트 루트 폴더에 있어야 합니다. (현재 적용 완료됨)
- 처음 빌드 시 필요한 도구들을 다운로드하느라 시간이 조금 걸릴 수 있습니다.
